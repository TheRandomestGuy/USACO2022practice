#include <bits/stdc++.h>

using namespace std;
int N;
vector<int> arr;
int main(){
    cin >> N;
    int a;
    for(int i = 0; i < N; i++){
        cin >> a;
        arr.push_back(a);
    }

    int sub[N];
    sub[0] = 1;
    int max = 0;
    for(int i = 1; i < N; i++){
        if(){
            
        }
    }
}
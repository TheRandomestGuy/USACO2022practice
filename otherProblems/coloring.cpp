#include <bits/stdc++.h>

using namespace std;

int t, n, m, k;
vector<int> a;

int main(){
    cin >> t;
    int b;
    for(int j = 0; j < t; j++){
        cin >> n >> m >> k;
        for(int i = 0; i < m; i++){
            cin >> b;
            a.push_back(b);
        }
        sort(begin(a), end(a));
        while(a[0] > 0){
            for(int i = m - 1; ){

            }
        }
    }
}
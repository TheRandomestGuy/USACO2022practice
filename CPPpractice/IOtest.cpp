#include <bits/stdc++.h>

typedef long long ll;

using namespace std;
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    string s;
    getline(cin, s);
    cout << s << "\n";

    int x, y;
    while (cin >> x) {
        y = x + y;
        cout << y << "\n";
    }
}